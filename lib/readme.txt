This folder contains 3rd party code and external libraries used by the Mindshare Theme API.

In the future the `lib` folder will be fully deprecated in favor of the `vendor` folder with files managed using composer (see https://getcomposer.org for info).
